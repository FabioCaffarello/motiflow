vamos agora começar um planejamento bem completo pararefatorar o slot de sidenavbar para ser Start Components, e vamos ter componentes obrigatórios e opcionais, então precisamos de um catalogo de componentes, sende que os obrigatórios já são renderizados com as configurações default e para configurar vamos clicar no componente no preview e abrirá um drawer, com as configurações do componente para configurarmos.  a ideia é que a SideNavbar seja um componente obrigatório, o container também e podemos ter como opcional o card centro do container. a ideia é já popular o app com componentes básicos do system design. e que no catalogo só tenha disponível componentes das features liberadas inicialmente que pode ser a de home, com isso vamos ter componentes obrigatórios e opocionanis e vamos poder configurar os parametros clicando e alterando no drawer.